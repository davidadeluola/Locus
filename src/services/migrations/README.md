This folder is for descriptive migration scripts and notes. Actual SQL migrations live in the top-level `supabase/migrations/` folder.

Place migration helpers or ID generators here if you need to run client-assisted migrations.
